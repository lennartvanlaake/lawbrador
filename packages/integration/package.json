{
	"name": "@lawbrador/cypress",
	"version": "1.0.0",
	"license": "MIT",
	"type": "module",
	"dependencies": {
		"cypress": "9.1.0"
	},
	"scripts": {
		"it": "cypress run",
	        "sync": "watch 'rsync -a workspace:~/lawbrador/packages/integration /home/lennart/lawbrador/packages' &>/dev/null &",  
                "open": "yarn sync && cypress open"
	}
}
