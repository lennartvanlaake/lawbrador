{
	"name": "@lawbrador/shared",
	"version": "1.0.0",
	"license": "AGPL-3.0-only",
	"type": "module",
	"scripts": {
		"test": "ts-mocha -p tsconfig.json src/**/*.spec.ts",
		"test:build": "tsc --noEmit",
		"test:debug": "ts-mocha -p tsconfig.json src/**/*.spec.ts --inspect-brk",
		"test:watch": "ts-mocha -p tsconfig.json --reporter min --watch --watch-files src/**/*.spec.ts src/**/*.spec.ts",
		"build": "tsc --build"
	},
	"dependencies": {
		"@sinclair/typebox": "^0.20.5",
		"@types/expect": "^24.3.0",
		"@types/mocha": "^9.0.0",
		"ajv": "^8.8.2",
		"ajv-formats": "^2.1.1",
		"chai": "^4.3.4",
		"dotenv": "^14.3.2",
		"mongodb": "^4.1.4",
		"ts-mocha": "^8.0.0"
	},
	"devDependencies": {
		"@types/chai": "^4.3.0",
		"@types/chai-as-promised": "^7.1.4",
		"@types/mocha": "^9.0.0",
		"typescript": "^4.5.4"
	}
}
